 function squareOfWordLength(str) {
    var word=str.length;
    console.log(`givien the length of string:${word}`);
    
    var square=word*word;
   console.log(`given the square of string:${square}`);
    
    return square;
 }
 squareOfWordLength("JavaSCript");

squareOfWordLength("Google Chrome");

 squareOfWordLength("Web Developer Smart");
 
 function wordLength() {
    var str = "I Am Ui Developer";
    var word=str.length;
    console.log(`length of string: ${word}`);    
    var ap=str.split(" ").length;
    console.log(`availabl word is:${ap}`);
    var result=word/ap;

    console.log(`final result:${result}`);
    
 }
 wordLength();



var greaternumber= function () {
   
}
greaternumber(10, )